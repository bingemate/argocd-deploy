apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name}}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
    - port: {{ .Values.service.wsPort }}
      targetPort: 3001
  selector:
    app: {{ .Chart.Name}}
  type: {{ .Values.service.type }}
